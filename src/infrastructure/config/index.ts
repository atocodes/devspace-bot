import { ALLOWED_CHAT_IDS } from "./allowedChats.config";
import {
  BOTOKEN,
  GEMINI_TOKEN,
  NODE_ENV,
  OLLAMA_API_KEY,
  PORT,
} from "./env.config";
import { logger } from "./logger.config";

export {
  logger,
  BOTOKEN,
  GEMINI_TOKEN,
  PORT,
  OLLAMA_API_KEY,
  NODE_ENV,
  ALLOWED_CHAT_IDS,
};
